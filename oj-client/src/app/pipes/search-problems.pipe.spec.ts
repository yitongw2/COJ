import { SearchProblemsPipe } from './search-problems.pipe';

describe('SearchProblemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProblemsPipe();
    expect(pipe).toBeTruthy();
  });
});
